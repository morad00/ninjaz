name: Build and Notify

on:
  push:
    branches: ["main"]

jobs:
  build:
    if: "contains(github.event.head_commit.message, '[run-ci]')"
    runs-on: self-hosted

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v2

#      - name: Flutter clean
#        run: flutter clean
#
#      - name: Install dependencies
#        run: flutter pub get
#
#      - name: Build dart runners
#        run: dart run build_runner build
#
#      - name: Run Flutter Tests
#        run: flutter test
#
#      - name: Build APK
#        run: flutter build apk --release

      - name: Send APK Release via Mail (SMTP)
        uses: hilarion5/send-mail@v1
        with:
          smtp-server: smtp.gmail.com
          smtp-port: 465
          smtp-secure: true
          from-email: moh.mourad9519@gmail.com
          to-email: mohamed.murad.taha@gmail.com
          username: "Github"
          password: "nwhk oirn aldp laoq"
          subject: Test Email
          body: This is a test email sent from GitHub Actions.
          html: |
            <h1>This is a test email</h1>
            <p>Test Paragraph</p>